const path = require("path");

module.exports = {
  "seeders-path": path.resolve("libs/database", "seeders"),
  "migrations-path": path.resolve("libs/database", "migrations"),
  config: path.resolve("libs/database", "config.js"),
};
